# v信 容量验收报告  (20260806_142315)
目标: **10万条消息/天** + **1000人同时在线**  ·  模式: `check`
服务器: `meta-cats-3.localdomain`  ·  2核 / 2.0Gi 内存

---

## 1. 服务存活
✅ PM2 `vxin-server-v2` 在线实例数: 3
✅ Nginx 运行中
✅ 后端 API 可达 (/api/auth/me → 401 未授权，符合预期)

---

## 2. 资源余量（2GB 小机是主要瓶颈）
内存: 可用 1487MB / 总 2024MB
✅ 内存余量充足 (≥400MB)
负载: 0.08 (核数 2)
✅ CPU 负载健康
✅ 磁盘使用 34%

---

## 3. OS 上限（1000 并发连接的硬门槛）
当前 shell open-files 软上限: 65535
✅ open-files 上限充足
net.core.somaxconn = 4096
✅ somaxconn 充足

---

## 4. SQLite 数据库（消息吞吐核心）
✅ journal_mode=WAL (并发读写已优化)
✅ connection.js 已显式设置 busy_timeout (并发锁等待)
✅ synchronous=NORMAL (WAL 下安全且更快)
✅ 数据库完整性检查通过
✅ messages(conversation_id) 索引存在

---

## 5. Socket.IO 集群一致性 ⚠️ 最高风险项
PM2 实例数: 1  ·  代码含 socket.io 适配器: 否  ·  Redis: 运行中
✅ 单实例运行，无跨实例投递问题

---

## 6. Nginx 反代（1000 WS 经此路径的真实瓶颈）
✅ 已设 proxy_read_timeout（长连接不会被 60s 默认值掐断）
worker_connections=8192 · worker_processes=auto
✅ worker_connections 充足（≥8192，足以承载 1000 WS 双向代理）

---

## 7. 压力测试（实测吞吐 & 并发在线）
⚠️  缺少 socket.io-client（并发长连接实测必需）
参数: 并发连接=300  发消息速率=20/s  时长=20s
cleanup: 无测试账号
group: 测试群 __lt_group__ 已含 300 名成员
create: 已播种 300 个测试账号 (phone LT000000000..LT000000299, 密码 Loadtest1234)

_压测开始: 目标=http://127.0.0.1:3002 并发=300 soak=90s · socket.io-client=缺失_

### A. 并发登录 (300 账号, 每批 25, 直连)
- 成功 300 / 失败 0  ·  成功率 100.0%
- 登录延迟(含bcrypt) p50=2690ms  p95=2787ms  max=2806ms
- ✅ 认证链路稳定，会话池就绪
### B. HTTP 写入吞吐 + 写延迟 (持久化路径)
- 尝试 395 / 20s · 成功 395 · 限流429 0 · 真实错误 0
- 写延迟 p50=17ms  p95=34ms  max=85ms
- ✅ 服务端零真实错误 
- 目标 10万/天=1.16条/秒，远低于服务端能力(写 p95=34ms) → 吞吐充足
### C. 并发长连接
- ❌ 跳过：未装 socket.io-client

_压测结束_
cleanup: 已删除 300 个测试账号及其会话/消息
   已清理压测临时账号

---

## 结论
通过 **16** · 警告 **1** · 失败 **0** · 自动修复 **0**
🟡 **基本达标**：有 1 项需关注（多为容量余量/Nginx超时），见上文。

---
### 附录A — server.js 接 Redis 适配器（多实例一致投递）
```js
const { createClient } = require('redis');
const { createAdapter } = require('@socket.io/redis-adapter');
const pub = createClient({ url: 'redis://127.0.0.1:6379' });
const sub = pub.duplicate();
await Promise.all([pub.connect(), sub.connect()]);
io.adapter(createAdapter(pub, sub));   // 在 new Server(...) 之后
```

### 容量速算（供参考）
- 10万条/天 = 1.16 条/秒均值；按 15× 峰值 ≈ 18 条/秒。SQLite(WAL) 可达数千写/秒 → 吞吐无压力。
- 1000 在线 = 1000 条 WebSocket。内存 ~50-80MB、单核可撑；真正的门槛是
  ① OS 句柄上限(已修) ② 跨实例投递一致性(第5节) ③ 2GB 内存余量。

报告已保存: /root/v信/ops/reports/capacity_20260806_142315.md
