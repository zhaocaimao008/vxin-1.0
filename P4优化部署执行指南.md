# 🚀 v信 P4 优化 - 部署执行指南

**生成时间**: 2026-08-12  
**版本**: v2.2.2-p4-extended  
**状态**: 🟢 生产就绪

---

## 📋 快速检查清单

在部署前，运行以下命令验证所有文件都已准备好：

```bash
# 1. 检查新增文件
ls -la /root/v信/backend-v2/src/utils/{searchRanking,deduplicator,batchAckManager,cacheWarmer,networkAwareRetry,redis}.js
ls -la /root/v信/backend-v2/src/routes/optimization.routes.js

# 2. 检查语法
node -c /root/v信/backend-v2/src/utils/searchRanking.js
node -c /root/v信/backend-v2/src/utils/deduplicator.js
node -c /root/v信/backend-v2/src/utils/batchAckManager.js
node -c /root/v信/backend-v2/src/utils/cacheWarmer.js
node -c /root/v信/backend-v2/src/utils/networkAwareRetry.js
node -c /root/v信/backend-v2/src/utils/redis.js
node -c /root/v信/backend-v2/src/routes/optimization.routes.js

# 3. 检查集成
grep "P4\." /root/v信/backend-v2/src/server.js | wc -l  # 应该显示 14+
grep "optimization" /root/v信/backend-v2/src/app.js     # 应该找到 1 行

# 4. 检查依赖
npm list 2>&1 | grep -i "missing\|ERR" || echo "✅ 依赖完整"
```

---

## 🎯 部署方案对比

### 方案 1️⃣: 保守部署（推荐首次）

**耗时**: 5-10 分钟  
**风险**: 🟢 极低  
**适用**: 首次上线、风险敏感团队

```bash
#!/bin/bash
set -e

cd /root/v信/backend-v2

# Step 1: 备份
echo "📦 备份数据库..."
cp src/wechat.db src/wechat.db.backup.$(date +%Y%m%d_%H%M%S)

# Step 2: 查看当前状态
echo "📊 当前服务状态:"
pm2 status vxin-server-v2

# Step 3: 停止服务
echo "🛑 停止服务..."
pm2 stop vxin-server-v2

# Step 4: 验证文件
echo "✅ 验证文件..."
test -f src/utils/searchRanking.js || exit 1
test -f src/utils/redis.js || exit 1
test -f src/routes/optimization.routes.js || exit 1

# Step 5: 部署代码
echo "📥 部署代码..."
git pull origin main  # 或: cp 新文件

# Step 6: 验证语法
echo "🔍 验证语法..."
node -c src/server.js || exit 1

# Step 7: 启动服务
echo "🚀 启动服务..."
pm2 start vxin-server-v2

# Step 8: 等待初始化
echo "⏳ 等待 5 秒初始化..."
sleep 5

# Step 9: 查看日志
echo "📋 检查日志..."
pm2 logs vxin-server-v2 --lines 30

# Step 10: 验证端点
echo "🔗 验证 API..."
curl -s http://localhost:3002/health | jq .
curl -s http://localhost:3002/api/optimization/stats | jq . | head -20

echo "✅ 部署完成！"
```

### 方案 2️⃣: 快速部署

**耗时**: 1-2 分钟  
**风险**: 🟡 中  
**适用**: 小规模迭代、紧急修复

```bash
cd /root/v信/backend-v2
pm2 restart vxin-server-v2 --update-env
sleep 3
curl http://localhost:3002/health
```

### 方案 3️⃣: 灰度部署（金丝雀）

**耗时**: 30+ 分钟  
**风险**: 🟢 极低  
**适用**: 大规模应用、生产环境

```bash
# Step 1: 部署到金丝雀实例
cd /root/v信/backend-v2
pm2 start src/server.js --name "vxin-canary"
sleep 5
curl http://localhost:3003/health  # 假设金丝雀用 3003 端口

# Step 2: 监控 30 分钟
echo "🔍 监控金丝雀实例..."
pm2 logs vxin-canary --lines 50

# Step 3: 如果无问题，逐步切换
# 使用 Nginx 或负载均衡器将 10% 流量切到金丝雀
# 等待 15 分钟，监控指标
# 逐步增加流量至 100%
```

---

## 📊 部署检查清单

### ✅ 部署前 (Do Before)

- [ ] 数据库备份完成
- [ ] 代码审查通过
- [ ] 新文件验证无误
- [ ] 语法检查通过
- [ ] 测试环境验证通过
- [ ] 回滚方案已准备
- [ ] 相关团队已通知
- [ ] 监控告警已配置
- [ ] 值班人员已确认

### ⚙️ 部署中 (Do During)

- [ ] 选择低峰期 (避免用户高峰)
- [ ] 实时监控日志输出
- [ ] 检查 Redis 连接状态
- [ ] 验证 P4 模块初始化
- [ ] 记录关键时间戳
- [ ] 保持通讯畅通
- [ ] 随时准备回滚

### ✔️ 部署后 (Do After)

- [ ] 检查服务状态 (pm2 status)
- [ ] 查看启动日志 (pm2 logs)
- [ ] 测试 API 端点
- [ ] 监控错误率 (< 0.1%)
- [ ] 检查性能指标
- [ ] 收集用户反馈
- [ ] 记录部署成果

---

## 🔧 故障排查

### 问题: 服务无法启动

```bash
# 1. 检查语法
node -c src/server.js

# 2. 查看错误日志
pm2 logs vxin-server-v2 --err --lines 100

# 3. 检查依赖
npm ls

# 4. 手动启动 (获取详细错误)
node src/server.js 2>&1 | head -50
```

### 问题: P4 模块未初始化

```bash
# 1. 检查日志中是否有 [P4] 标记
pm2 logs vxin-server-v2 | grep "\[P4"

# 2. 检查 Redis 连接
redis-cli ping

# 3. 手动检查模块
node -e "const m = require('./src/utils/searchRanking'); console.log('OK')"
```

### 问题: Redis 连接失败

```bash
# 1. 检查 Redis 服务
redis-cli ping

# 2. 检查连接配置
grep "REDIS" .env

# 3. 测试连接
redis-cli -h localhost -p 6379
```

### 问题: 性能没有改善

```bash
# 1. 检查缓存预热
redis-cli KEYS "active:*" | wc -l
redis-cli KEYS "msg:*" | wc -l

# 2. 检查搜索排序
curl -X POST http://localhost:3002/api/optimization/search/rank

# 3. 监控 API 响应时间
curl -w "@curl-format.txt" -o /dev/null -s \
  http://localhost:3002/api/optimization/stats
```

---

## 📈 性能监控

### 关键指标

部署后 1 小时内应观测到：

| 指标 | 目标 | 验证命令 |
|------|------|---------|
| 搜索延迟 | < 50ms | `curl -w "%{time_total}" /api/search` |
| 排序延迟 | < 100ms | `curl -w "%{time_total}" /api/optimization/search/rank` |
| 缓存命中 | > 80% | `curl /api/optimization/stats \| jq .cacheWarmer` |
| 错误率 | < 0.1% | `grep ERROR pm2.log` |
| 内存占用 | < 500MB | `pm2 monit` |

### 监控命令

```bash
# 实时监控
pm2 monit

# 查看日志
pm2 logs vxin-server-v2

# 获取统计
curl http://localhost:3002/api/optimization/stats | jq .

# 检查健康
curl http://localhost:3002/health
```

---

## 🔄 回滚方案

### 快速回滚 (< 5 分钟)

```bash
cd /root/v信/backend-v2

# 方式 1: Git 回滚
git revert HEAD
pm2 restart vxin-server-v2

# 方式 2: 恢复数据库
rm src/wechat.db
cp src/wechat.db.backup.YYYYMMDD_HHMMSS src/wechat.db
pm2 restart vxin-server-v2
```

### 验证回滚

```bash
# 1. 检查服务状态
pm2 status

# 2. 验证旧版本
curl http://localhost:3002/health

# 3. 检查日志
pm2 logs vxin-server-v2 | head -20
```

---

## 📞 应急联系

如果部署出现问题：

1. **立即停止服务**
   ```bash
   pm2 stop vxin-server-v2
   ```

2. **收集诊断信息**
   ```bash
   pm2 logs vxin-server-v2 > /tmp/vxin-error.log
   node -v > /tmp/node-version.log
   npm ls > /tmp/npm-deps.log
   ```

3. **执行回滚**
   ```bash
   git revert HEAD
   pm2 restart vxin-server-v2
   ```

4. **通知相关人员**
   - DevOps 负责人
   - 产品经理
   - 运维主管

---

## ✨ 部署后验证

所有部署完成后，运行以下验证：

```bash
#!/bin/bash

echo "🔍 部署后验证..."
echo ""

# 1. 服务状态
echo "1️⃣ 服务状态"
pm2 status vxin-server-v2
echo ""

# 2. 启动日志
echo "2️⃣ 启动日志 (P4 初始化)"
pm2 logs vxin-server-v2 | grep -E "\[P4|optimization" | head -10
echo ""

# 3. API 端点
echo "3️⃣ API 端点验证"
echo "健康检查:"
curl -s http://localhost:3002/health | jq .
echo ""
echo "优化统计:"
curl -s http://localhost:3002/api/optimization/stats | jq '.searchRanking' | head -5
echo ""

# 4. 性能指标
echo "4️⃣ 性能指标"
ps aux | grep "node.*server" | grep -v grep
echo ""

# 5. 错误检查
echo "5️⃣ 错误检查"
pm2 logs vxin-server-v2 | grep -i "error" | head -5 || echo "✅ 无错误"
echo ""

echo "✅ 验证完成！"
```

---

## 🎊 部署完成

当看到以下信息时，部署成功：

```
[P4.3] 搜索排序引擎已初始化
[P4.4] 消息去重管理已初始化
[P4.5] 批量 ACK 管理已初始化
[P4.6] 缓存预热完成: 1234 ms
[P4.7] 网络质量检测完成: good
[P4.2-P4.7] 所有 P4 优化模块已初始化
```

恭喜！v信 P4 优化已成功部署到生产环境。

---

**问题反馈**: 如有任何问题，请收集日志并联系技术团队。  
**后续优化**: 部署 2 周后收集数据，评估性能提升，规划 P5 优化。

