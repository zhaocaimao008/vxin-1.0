# eslint 9 与 eslint-plugin-react/@eslint-community 的 peer 声明冲突（历史遗留），
# 用 legacy 解析绕过。影响：npm ci / npm install 在服务器部署与 CI 上一致可用，
# 不会因 ERESOLVE 中断 web 构建。仅影响依赖树解析策略，不改变已锁定的 lock 版本。
legacy-peer-deps=true
