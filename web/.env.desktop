# ── Electron desktop 构建专属环境变量 ──────────────────────────
# 此文件由 vite build --mode desktop 读取，不影响普通 web 构建

# 后端服务器地址（打包进 JS bundle，Axios / Socket.io 使用此地址）
# 仅在 VXIN_LOAD_LOCAL=1（本地文件模式）时需要；
# 默认的"远程加载模式"直接加载服务端页面，此变量无需设置。
VITE_SERVER_URL=http://104.244.95.70:8086
