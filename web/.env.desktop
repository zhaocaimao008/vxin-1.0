# ── Electron desktop 构建专属环境变量 ──────────────────────────
# 此文件由 vite build --mode desktop 读取，不影响普通 web 构建
#
# 注意：v2.0.0 起，v信 从远程配置服务器读取服务器地址，
# 不再需要硬编码 VITE_SERVER_URL。
# 此文件仅保留 Electron 构建标记，实际服务器地址从
# https://config.vxin.com/config.json 获取。
