# R19 · 构建/CI/部署
## CI workflows
android-build.yml
android-release.yml
ci.yml
deploy.yml
e2e-web.yml
ios-build.yml
ios-testflight.yml
windows-build.yml
## 构建脚本 (web package.json scripts)
```
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "preview": "vite preview",
    "dev:desktop": "cross-env ELECTRON_DEV=1 concurrently -k --names \"VITE,ELECTRON\" -c \"cyan,green\" \"vite --port 3000\" \"wait-on http://localhost:3000 && electron .\"",
    "dev:desktop:headless": "cross-env ELECTRON_DEV=1 concurrently -k --names \"VITE,ELECTRON\" -c \"cyan,green\" \"vite --port 3000\" \"wait-on http://localhost:3000 && xvfb-run --auto-servernum electron .\"",
    "build:desktop": "vite build --mode desktop && electron-builder --linux",
    "build:desktop:headless": "vite build --mode desktop && electron-builder --linux",
    "build:desktop:win": "vite build --mode desktop && electron-builder --win",
    "build:desktop:mac": "vite build --mode desktop && electron-builder --mac",
    "pack:desktop": "vite build --mode desktop && electron-builder --dir",
    "dev:mobile": "ionic serve",
    "build:mobile": "npm run build && cap sync",
    "build:android": "npm run build && cap add android && cap sync android && cap open android",
    "build:ios": "npm run build && cap add ios && cap sync ios && cap open ios",
    "android:build": "cd android && ./gradlew assembleRelease",
```
## source map 泄漏风险 (dist 是否含 .map)
dist .map 文件: 0
## .env 是否被 gitignore
24:Thumbs.db
52:.deploy-config/github-secrets.txt
53:*keystore*
56:*.keystore
61:# 环境文件（*.env / .env.local 等），仅保留 .env.example
62:.env
63:.env.*
64:!.env.example
65:!*.env.example
## 敏感文件是否入库 (git 追踪)
```
android/app/src/main/java/com/vxin/app/feature/auth/ForgotPasswordScreen.kt
android/app/src/main/java/com/vxin/app/feature/auth/ForgotPasswordViewModel.kt
desktop-electron/src/update-public-key.pem
ios/Vxin/Features/Auth/ForgotPasswordView.swift
ios/Vxin/UI/Components/PasswordField.swift
web/src/pages/ForgotPassword.jsx
```
## 部署脚本
/root/v信/deploy.sh

/root/v信/deploy/:
README.md
nginx-gray-release.conf
nginx-vxin.conf.example
nginx.conf.template
rollback.sh
setup-coturn.sh
setup-new-server.sh
## 建议: 生产关闭 sourcemap 或不发布；确认 .env/keystore 不入库；CI 加 lint+test 门禁
