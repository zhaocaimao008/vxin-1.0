# R3 · 无障碍 A11y

| 指标 | 数量 |
|---|---|
| <button> | 177 |
| aria-* | 193 |
| role= | 167 |
| 可点击 <div onClick> | 73 |
| <img> 缺 alt | 3 |

## 可点击 div 缺 aria/role (样例)
```
utils/toast.jsx:40:        <div className="wc-confirm-overlay" onClick={e => { if (e.target === e.currentTarget) { confirmState.resolve(false); setConfirm(null); } }}>
components/Moments.jsx:474:        <div className="wc-modal-overlay" onClick={e => e.target === e.currentTarget && setShowSettings(false)}>
components/Moments.jsx:501:        <div className="wc-modal-overlay" onClick={e => e.target === e.currentTarget && setShowFriendPicker(false)}>
components/Moments.jsx:537:        <div className="wc-modal-overlay" onClick={e => e.target === e.currentTarget && setNotifList(null)}>
components/GroupInfo.jsx:795:        <div className="wc-modal-overlay" onClick={e => e.target === e.currentTarget && setShowQR(false)}>
components/GroupInfo.jsx:827:        <div className="wc-modal-overlay" onClick={e => e.target === e.currentTarget && setShowInvite(false)}>
components/Profile.jsx:74:    <div className={`wc-crow${onClick ? ' wc-crow-clickable' : ''}`}
components/Profile.jsx:978:        <div className="wc-modal-overlay" onClick={() => setShowQR(false)}>
components/Profile.jsx:1107:        <div className="wc-modal-overlay" onClick={() => setShowQR(false)}>
components/ChatWindow.jsx:2141:                <div key={c.id} onClick={() => sendContactCard(c)}
components/ChatWindow.jsx:2491:              <div className="wc-ctx-item" onClick={() => ctxAction('collect')}>收藏</div>
components/AddFriendModal.jsx:99:      <div className="afm-overlay" ref={trapRef} onClick={e => e.target === e.currentTarget && onClose()}>
components/GlobalSearch.jsx:158:            <div key={c.id} className="gs-row" onClick={() => openContact(c)}
components/GlobalSearch.jsx:185:            <div key={g.id} className="gs-row" onClick={() => openConversation(g)}
components/GlobalSearch.jsx:209:            <div key={m.id} className="gs-row" onClick={() => openMessageLocation(m)}
components/RedPacketModal.jsx:49:    <div className="rpm-overlay" ref={trapRef} onClick={() => { if (!sending) onClose(); }}>
components/VoicePlayer.jsx:70:    <div className="wc-msg-voice-player wc-voice-player" onClick={e => e.stopPropagation()}>
components/CallHistory.jsx:78:            <div key={c.id} data-testid="call-log-item" onClick={() => openPeer(c)}
components/ChatList.jsx:391:            <div className="wc-ctx-item" onClick={() => toggleMarkUnread(ctxMenu.conv)}
components/ChatList.jsx:396:            <div className="wc-ctx-item" onClick={() => pin(ctxMenu.conv, !ctxMenu.conv.pinned)}
components/ChatList.jsx:401:            <div className="wc-ctx-item" onClick={() => mute(ctxMenu.conv, !ctxMenu.conv.muted)}
components/ChatList.jsx:407:            <div className="wc-ctx-item danger" onClick={() => deleteConv(ctxMenu.conv)}
components/UserProfile.jsx:133:    <div className="up-overlay" onClick={onClose}>
components/UserProfile.jsx:144:    <div className="up-overlay" ref={trapRef} onClick={e => e.target === e.currentTarget && onClose()}>
components/IconButton.jsx:5: * 统一替代散落的 `<div onClick>` 图标按钮：
```
## <img> 缺 alt (样例)
```
components/MessageItem.jsx:172:                <img loading="lazy"
components/AuthImage.jsx:5:// Web 端 <img> 靠同源 cookie 能直接加载；但 Electron 桌面端 <img> 无法携带
components/ImagePreview.jsx:160:      <img
```
## 建议
- 图标按钮统一 <IconButton aria-label>；可点击 div → button + Enter/Space
- 弹窗遮罩加 role=button + 键盘事件；img 补 alt
