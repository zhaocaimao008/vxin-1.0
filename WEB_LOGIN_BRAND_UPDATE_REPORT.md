# V信 Web Login Brand Update Report

审核人：Claude Code（Principal Engineer 角色）
日期：2026-08-24
Commit：`3d7b7ab`（本地未 push）

## 1. 问题

上一轮任务只换了 Logo 图形和 favicon，登录页其余部分（背景、按钮、Tab、链接）仍是原来的微信绿风格，整体看起来是"贴了张新 Logo 的旧页面"，不是统一的黑金品牌。

## 2. 改动范围

只改了 `web/src/styles/login.css`（Login.jsx 和 Register.jsx 共用同一份样式表）+ Login.jsx 里 1 处内联样式，没有新建组件、没有改任何 `.jsx` 的结构/逻辑。

### 2.1 左侧品牌区域

| 项 | 原来 | 现在 |
|---|---|---|
| 背景 | 浅薄荷绿渐变 | 黑色渐变（`#050505→#0d0b08→#000`）+ 两组交叉极细金色网格线（~3% 透明度，"科技线条"）+ Logo 后方柔和金色光晕 |
| 粒子点阵 | 绿色 | 金色（原有的 `.auth-split-left-dots` 装饰层直接换色，没加新元素） |
| Logo / "V信" 文字 / 标语 | 不变（上轮已换） | 位置不变，标题改白色、标语改 `#CCCCCC`（品牌规范里定义的辅助文字色），适配新的黑底 |

### 2.2 登录区域（右侧表单）

| 元素 | 原来 | 现在 |
|---|---|---|
| 背景 | 纯白 | 深色 `#0d0d0d`（卡片）/ 输入框 `#141414` |
| Tab 选中态 | 绿色文字+绿色下划线 | 金色文字+金色下划线 |
| 输入框聚焦 | 绿色描边 | 金色描边 |
| 提交按钮（登录/注册） | 实心绿底白字 | **黑底金字**（`#141414` 底 + `1.5px` 金色描边 + 柔光），按你的要求做的描边风格而不是实心金底——见下方截图，禁用态/填写后启用态都截图核对过 |
| 勾选框（记住密码/用户协议） | 绿色 | 金色 |
| 各类链接（忘记密码/立即注册/立即登录/用户协议/隐私政策/语言菜单） | 绿色 | 金色 |
| 错误提示（红色） | 红色 | **未改**——功能色，不属于品牌色范畴，保持红色语义清晰 |

### 2.3 深色模式（body.dark-mode）

原来这里是另一套独立的绿色配色（App 自带的亮/暗主题切换）。既然登录页现在默认就是黑底，切到 dark-mode 时如果不改这块，点击切换会从"黑金"退回"绿色"，是个明显的不一致。已经把这块里的绿色强调色也统一换成了金色，两种模式视觉一致。

### 2.4 未改动

- 登录/注册表单结构、字段校验、提交逻辑、API 调用、路由跳转、权限判断——一律未动
- `--color-primary` 这个全局 CSS 变量（App 内聊天气泡/按钮/设置页等到处在用）**完全没碰**，只是把 Login.jsx 里那一处直接读这个全局变量的内联绿色文字，改成了写死的 `#FFD700`，不再挂在全局 token 上——这样改的是这一处文字颜色，不会把整个 App 的主题带绿变金
- 错误提示的红色（功能语义色）

## 3. 截图验证

用 `npx playwright screenshot` 打真实线上地址（已随附件发送）：

1. `https://vxinchat.com/app/login`（默认态）
2. `https://vxinchat.com/app/register`（默认态）
3. 登录页填写手机号+密码+勾选协议后的截图——用 Playwright 脚本填表触发按钮**启用态**（未真实提交，没有走真实登录请求），确认"黑底金字描边"按钮在两种状态（禁用/启用）下都符合预期：禁用态是近黑底+暗金字（几乎融入背景，正确的禁用观感），启用态是黑底+清晰金色描边和金色文字。

## 4. 部署

`npm run build` → `rsync` 到 `/var/www/vxin-web/app/`。CSS/JS 文件名按内容 hash 命名，浏览器/Service Worker 会当新资源处理，不需要再动 `SW_VERSION`。
