[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 显示详细输出
addopts =
    -v
    --tb=short
    --no-header
    -p no:warnings

# 用例执行顺序：文件内按定义顺序
# （TC-OFF-08 修改 DB 后还原，不影响其他用例）
