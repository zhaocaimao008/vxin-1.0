import SwiftUI

extension Color {
    /// v信 品牌绿（对齐 Web/Android #07C160）
    static let vxinGreen = Color(red: 0x07 / 255, green: 0xC1 / 255, blue: 0x60 / 255)
    /// 我的聊天气泡：微信浅绿（对齐 Web #95EC69，原先误用品牌深绿）
    static let vxinBubbleMine = Color(red: 0x95 / 255, green: 0xEC / 255, blue: 0x69 / 255)
    /// 浅绿/白气泡上的深色文字（对齐 Web #1A1A1A）
    static let vxinBubbleText = Color(red: 0x1A / 255, green: 0x1A / 255, blue: 0x1A / 255)
    static let vxinTextSecondary = Color(red: 0x88 / 255, green: 0x88 / 255, blue: 0x88 / 255)
    static let vxinError = Color(red: 0xFA / 255, green: 0x51 / 255, blue: 0x51 / 255)
}
