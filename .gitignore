# Desktop Electron
desktop-electron/node_modules/
desktop-electron/dist/
desktop-electron/*.exe
desktop-electron/*.dmg
desktop-electron/*.AppImage
desktop-electron/*.deb
desktop-electron/release/

# Capacitor Mobile
mobile/node_modules/
mobile/android/
mobile/ios/
mobile/web-dist/
mobile/*.apk
mobile/*.aab

# Web build output
web/dist/
web/node_modules/

# OS
.DS_Store
Thumbs.db
*.log

# 构建产物 / 安装包（别进 git，太大）
*.exe
downloads/
backend-v2/downloads/
landing/public/downloads/

# 用户上传文件（运行时数据，别进 git）
backend/uploads/
backend-v2/uploads/

# SQLite 数据库（运行时数据，含用户数据，绝不入库）
*.db
*.sqlite
*.sqlite3
*.db-wal
*.db-shm
*.db-journal

# 源码备份
*.bak

# 独立配置仓库（自带 .git，单独管理）
vxin-config/

# 图标生成临时目录
.icon-build/

# 机器人审计/UI 快照产物（含源码备份，一次性生成物，不入库）
.robot-audit/
.robot-audit-20/
.robot-ui-20/
.robot-ui-5/
.android-web-parity-audit.md
.tri-platform-audit-*.md

# 任何 node_modules（兜底：防止子目录 node_modules 被误提交撑爆仓库）
node_modules/
**/node_modules/

# 🔒 机密：私钥 / 部署密钥 / 密钥库（绝不入库；泄漏需立即轮换）
.deploy-config/keys/
.deploy-config/github-secrets.txt
*keystore*
*.jks
*.p12
*.keystore
id_rsa
id_ed25519
*-deploy
!*-deploy.sh
# 环境文件（*.env / .env.local 等），仅保留 .env.example
.env
.env.*
!.env.example
!*.env.example

# e2e 测试产物
e2e/node_modules/
e2e/test-results/
e2e/playwright-report/
/tmp/vxin-e2e-*
e2e/fixtures/bigfile.txt
e2e/.e2e-state.json
e2e/appium/__pycache__/
e2e/shots/
e2e/playwright/web/_shots.spec.js

# Hermes 部署日志（临时，归档到 .hermes-logs-archive/，不入库）
hermes-deploy-*.log
.hermes-logs-archive/

# iOS 生成产物：Vxin.xcodeproj 由 XcodeGen 从 project.yml 每次重新生成，
# build/ 是 xcodebuild 输出，均不入库（防本地跑 xcodegen 后误提交）。
ios/build/
ios/Vxin.xcodeproj/
scripts/__pycache__/
.ios-review-last
.ios-review.log
# 内部任务书(Claude 开发批次指令)
.claude-briefs/

# 生产凭据目录（APNs .p8 / Firebase admin / GeTui）——禁止入库
backend-v2/.secrets/
backups/
backend-v2/backups/
