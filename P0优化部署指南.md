# P0 优化部署指南

## 🎯 已完成的优化项

### 1. ✅ Rate Limiting 全局限流
- 状态：已部署，无需额外操作
- 覆盖：21 个限流器保护所有关键 API
- 存储：Redis（共享）+ 内存（降级）

### 2. ✅ 数据库自动备份
- 状态：已部署，正在运行
- 频率：每小时一次
- 保留：7 天自动清理
- 位置：`/root/backup/vxin/`
- 日志：`/var/log/vxin-backup.log`

### 3. ✅ Sentry 错误监控集成
- 状态：代码已集成，需配置 DSN
- 功能：错误捕获 + 性能追踪 + 会话回放

### 4. ✅ 敏感信息脱敏
- 状态：已实现工具函数
- 范围：前端 + 后端 + 日志

---

## 🚀 立即部署步骤

### 第一步：配置 Sentry DSN（可选）

如果需要错误监控，请执行：

```bash
# 1. 注册 Sentry 账号
# 访问 https://sentry.io 注册并创建项目

# 2. 配置后端 DSN
echo 'SENTRY_DSN=https://你的DSN@o0.ingest.sentry.io/项目ID' >> /root/v信/backend-v2/.env

# 3. 配置前端 DSN
echo 'VITE_SENTRY_DSN=https://你的DSN@o0.ingest.sentry.io/项目ID' >> /root/v信/web/.env.production
```

### 第二步：重新构建并部署

```bash
# 进入 Web 目录
cd /root/v信/web

# 构建前端（包含 Sentry 集成）
npm run build

# 部署到生产环境
sudo cp -r dist/* /var/www/dipsin.com/
sudo systemctl reload nginx

# 重启后端服务
cd /root/v信/backend-v2
pm2 restart vxin-server-v2
```

### 第三步：验证部署

```bash
# 1. 验证备份系统
/usr/local/bin/backup-vxin-db.sh
ls -lh /root/backup/vxin/

# 2. 验证 Cron 任务
crontab -l | grep backup

# 3. 验证后端服务
pm2 status
curl https://dipsin.com/health

# 4. 验证前端部署
curl -I https://dipsin.com/

# 5. 检查备份日志
tail -f /var/log/vxin-backup.log
```

---

## 📋 验证清单

### 必须验证项
- [x] 备份脚本可执行
- [x] Cron 任务已添加
- [x] 数据库路径正确
- [x] 前端代码已更新
- [x] 后端工具已创建

### 可选配置项
- [ ] Sentry DSN 已配置
- [ ] 前端重新构建
- [ ] 后端已重启
- [ ] OSS 备份已配置

---

## 🔧 配置文件位置

### 备份系统
- 脚本：`/usr/local/bin/backup-vxin-db.sh`
- 数据库：`/root/v信/backend-v2/wechat.db`
- 备份目录：`/root/backup/vxin/`
- Cron 配置：`crontab -e`

### 脱敏工具
- 前端：`/root/v信/web/src/utils/dataManipulation.js`
- 后端：`/root/v信/backend-v2/src/utils/dataMask.js`

### 监控配置
- Sentry 前端：`/root/v信/web/src/main.jsx`
- Sentry 后端：`/root/v信/backend-v2/src/utils/sentry.js`
- 限流配置：`/root/v信/backend-v2/src/middleware/rateLimiters.js`

---

## 📞 故障排查

### 备份失败
```bash
# 检查数据库文件
ls -lh /root/v信/backend-v2/wechat.db

# 手动执行备份
/usr/local/bin/backup-vxin-db.sh

# 查看详细日志
tail -20 /var/log/vxin-backup.log
```

### 限流不生效
```bash
# 检查 Redis 连接
redis-cli -h localhost -p 6379 ping

# 查看限流日志
pm2 logs vxin-server-v2 | grep RateLimit
```

### Sentry 未上报
```bash
# 检查环境变量
cat /root/v信/backend-v2/.env | grep SENTRY
cat /root/v信/web/.env.production | grep SENTRY

# 查看浏览器控制台是否有错误
# 访问 https://dipsin.com 打开 DevTools
```

---

## 🎉 完成标志

当以下所有条件满足时，P0 优化完全部署成功：

1. ✅ 每小时自动备份运行正常
2. ✅ 备份文件正常生成并压缩
3. ✅ 前端构建成功
4. ✅ 后端服务正常运行
5. ✅ Rate Limiting 正常工作
6. ✅ （可选）Sentry 正常上报错误

---

**部署完成后，请保留此文档以备后续参考。**

**下一阶段：P1 优化（CDN 加速、Redis 缓存、监控大盘）**
